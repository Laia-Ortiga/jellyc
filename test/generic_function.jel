module main

struct List {
    data *mut byte,
    length isize,
    capacity isize,
}

function push[T](list *mut List, value T, size `Size[T]) {
    
}

function main() {
    mut list = List(null, 0, 0)
    push(&list, 3, `size_of[i64])
}
