module std

import libc

public function print_byte(c i8) {
    libc.putchar(`zero_extend[c])
}

public function print_str(s @i8) {
    for i = <isize> 0; i < s.length; i += 1 {
        print_byte(s[i])
    }
}

function print_int_rec(x i64) {
    if x / 10 != 0 {
        print_int_rec(x / 10)
    }
    print_byte(<i8> `cast[x % 10] + '0')
}

public function print_int(x i64) {
    if x < 0 {
        print_byte('-')
        if x / -10 != 0 {
            print_int_rec(x / -10)
        }
        print_byte(<i8> `cast[-(x % 10)] + '0')
    } else {
        print_int_rec(x)
    }
}
